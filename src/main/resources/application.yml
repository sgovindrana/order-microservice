server:
  port: 9094

spring:
  profiles:
    active: dev
  application:
    name: ORDER-SERVICE  # Correct placement of application name
  data:
    mongodb:
      url: mongodb://localhost:27017/orderdb
      host: localhost
      port: 27017
      database: orderdb
      repositories:
        enabled: true


